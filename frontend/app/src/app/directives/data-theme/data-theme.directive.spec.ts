import { DataThemeDirective } from './data-theme.directive';

describe('DataThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new DataThemeDirective();
    expect(directive).toBeTruthy();
  });
});
