import { LeadingSlashPipe } from './leading-slash.pipe';

describe('LeadingSlashPipe', () => {
  it('create an instance', () => {
    const pipe = new LeadingSlashPipe();
    expect(pipe).toBeTruthy();
  });
});
